/* ============================================================
   角色配置 (Character Config)
   ------------------------------------------------------------
   这是全项目唯一需要手动维护的"数据源"。
   升级 / 调整角色时，只改这里的数字，页面会自动重新计算并渲染，
   无需改动任何逻辑代码（character_sheet.js）。

   ★ 派生值（属性调整值、熟练加值、先攻、法术豁免DC、法术攻击加成、
     生命维持治疗量等）由程序按 D&D 5e 公式自动算出，见 character_sheet.js
     的"派生数值计算"区块，无需在此手填。
============================================================ */
const CHAR = {

  /* ── 身份信息 ───────────────────────────────────────────── */
  name:      '金雳',        // 角色名
  race:      '丘陵矮人',    // 种族
  className: '牧师',        // 职业
  subclass:  '生命领域',    // 子职 / 神术领域
  level:     5,             // ★ 角色等级（升级时改这里，熟练加值等会自动重算）

  /* ── 六维属性得分（原始值；调整值程序自动计算 = ⌊(得分-10)/2⌋）── */
  abilities: {
    str:  6,   // 力量  → 调整值 -2
    dex: 10,   // 敏捷  → 调整值 +0
    con: 12,   // 体质  → 调整值 +1
    int: 11,   // 智力  → 调整值 +0
    wis: 17,   // 感知  → 调整值 +3（本角色施法关键属性）
    cha: 11,   // 魅力  → 调整值 +0
  },

  spellAbility: 'wis',      // 施法关键属性：法术豁免DC与法术攻击加成据此计算

  /* 法术豁免DC 默认走公式：8 + 熟练加值 + 施法属性调整值（当前 = 14）。
     如遇特殊情况需手动锁定，取消下一行注释即可覆盖。 */
  // spellSaveDC: 14,       // 手动覆盖法术豁免DC（可选）

  /* ── 战斗数值 ───────────────────────────────────────────── */
  maxHp: 37,                // 生命值上限
  ac:    18,                // 防御等级 AC
  speed: 25,                // 移动速度（尺）
  tempHpMax: 15,            // 临时HP滑条的最大可调值

  /* ── 施法资源 ───────────────────────────────────────────── */
  channelDivinity: 1,       // 引导神力次数（每次休息可用；牧师 6 级起为 2）
  spellSlots: [0, 4, 3, 2], // 各环阶法术位数量：index=环阶(1-3)，[0] 仅占位
  maxCantrips: 5,           // 已知戏法上限（牧师：1级3个 / 4级4个 / 10级5个）
  maxPrepared: 8,           // 可携带自选法术上限（牧师公式：感知调整值 + 牧师等级 = 3 + 5）

  /* 生命领域法术 — 常驻备法，玩家无法移除。key = 环阶 */
  domainSpells: {
    1: ['cure_wounds', 'bless'],                 // 1环：治疗真言、祝福术
    2: ['lesser_restoration', 'spiritual_weapon'],// 2环：次级复原术、灵体武器
    3: ['beacon_of_hope', 'revivify'],           // 3环：希望信标、活化术
  },

  /* ── 职业特性中的可变数值（随等级变化，会注入到页面文字里）── */
  channelHealPerLevel: 5,   // "生命维持"每牧师等级的治疗量系数（治疗量 = 5 × 等级）

  /* ── 角色信息 ───────────────────────────────────────────── */
  alignment: '守序中立',    // 阵营
  languages: '龙语、矮人语',// 已知语言

  /* 技能熟练列表：name=技能名，attr=关联属性 */
  skills: [
    { name: '历史', attr: '智力' },
    { name: '洞悉', attr: '感知' },
    { name: '医药', attr: '感知' },
    { name: '游说', attr: '魅力' },
  ],
};
